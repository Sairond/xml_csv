# xml_csv
1. —оздает 50 zip-архивов, в каждом 100 xml файлов со случайными данными следующей структуры:
<root><var name=ТidТ value=Т<случайное уникальное строковое значение>Т/><var name=ТlevelТ value=Т<случайное число от 1 до 100>Т/><objects><object name=Т<случайное строковое значение>Т/><object name=Т<случайное строковое значение>Т/>Е</objects></root>
¬ тэге objects случайное число (от 1 до 10) вложенных тэгов object.

2. ќбрабатывает директорию с полученными zip архивами, разбирает вложенные xml файлы и формирует 2 csv файла: ѕервый: id, level - по одной строке на каждый xml файл
¬торой: id, object_name - по отдельной строке дл€ каждого тэга object (получитс€ от 1 до 10 строк на каждый xml файл)

